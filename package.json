{
  "name": "agentgate",
  "version": "0.1.0",
  "description": "Guard MCP tool calls before they run.",
  "type": "module",
  "bin": {
    "agentgate": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "smoke:filesystem": "node examples/filesystem-smoke.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "agentgate": "tsx src/index.ts"
  },
  "keywords": [
    "mcp",
    "agents",
    "security",
    "firewall",
    "ai"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
